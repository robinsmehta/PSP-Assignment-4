#Can you create a identity matrix of shape (3,4). If yes write code for it.
'''An identity matrix is also a square matrix — meaning it has the same number of rows and columns 
(e.g., 3×3, 4×4). So, a true identity matrix of shape (3, 4) does not exist.
'''